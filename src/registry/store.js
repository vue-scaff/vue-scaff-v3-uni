export default ({}) => {
  const state = {
    alive: ['start'],
    oss: `//oss.joenix.com/vue-scaff`,
  };

  const mutations = {};

  const actions = {};

  return { state, mutations, actions };
};
